PROGRAM DICEX;
{$X+}
USES GRAPH, CRT,
     RAWSUPP, RGFSUPP, PALEFF, MAKEIMG, PLAYAREA, PLAYGAME;
VAR
  GRDRIVER: INTEGER;
  GRMODE: INTEGER;
  ERRCODE: INTEGER;
  X: INTEGER;
  OLDPAL: PALETTETYPE;
{  DIDGRAVITY: BOOLEAN;}
BEGIN
  GRDRIVER := VGA;
  GRMODE := VGAHI;
  INITGRAPH(GRDRIVER,GRMODE,'');
  ERRCODE := GRAPHRESULT;
  IF ERRCODE <> GROK THEN
  BEGIN
    WRITELN('Graphics error:', GRAPHERRORMSG(ERRCODE));
    HALT(1);
  END;
  CLEARDEVICE;
  DELAY(500);   { ALLOW TIME TO ENTER GRAPHICS MODE }

{  CLIPRAW2RGF('LOGO', 1,1,375,103); WRITE(#7);}

{  PALFADEOUT(OLDPAL);}
{  SHOWRGF('LOGO',(640-375) DIV 2, 1);}
{  PALFADEIN(OLDPAL);}

{  SHOWRAW('COLORS');}

  { MAKE THE LOGO IMAGE }
{  SHOWRAW('LOGO');
  WRITEIMG('LOGO', 1,1,375,103);}

  { MAKE DICE }
{  SHOWRAW('0DIE');}
{  WRITEIMG('0DIE', 1,1,26,27);}
{  SHOWRAW('1W');
  WRITEIMG('1W', 1,1,26,27);
  SHOWRAW('2W');
  WRITEIMG('2W', 1,1,26,27);
  SHOWRAW('3W');
  WRITEIMG('3W', 1,1,26,27);
  SHOWRAW('4W');
  WRITEIMG('4W', 1,1,26,27);
  SHOWRAW('5W');
  WRITEIMG('5W', 1,1,26,27);
  SHOWRAW('6W');
  WRITEIMG('6W', 1,1,26,27);}
{  SHOWRAW('BORDER');
  WRITEIMG('BORDER', 1,1,26,27);}

  { FILL DICE WITH SCREEN }
  READDICE;
  FILLWITHDICE(1500);
  PALFADEOUT(OLDPAL, TRUE);
  CLEARDEVICE;
  PALFADEIN(OLDPAL, FALSE);

{  PUTDIE(1,13,1);
  PUTDIE(2,13,2);
  PUTDIE(3,13,3);
  PUTDIE(4,13,1);
  PUTDIE(5,13,5);
  PUTDIE(6,13,4);
  PUTDIE(7,13,6);
  READLN;
  CHECKELIM;}

{  PUTDIE(5,1,5);}

  GAMELOOP;

{  STARTGRAVITY;
  REPEAT CHECKGRAVITY UNTIL KEYPRESSED;}

  READLN;
  CLOSEGRAPH;
END.

